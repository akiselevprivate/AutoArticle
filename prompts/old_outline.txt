Act as an SEO expert speaking fluent English to write an outline for an article titled: "{{title}}". The article topic is {{topic}}.

Return strictly only a JSON object that contains the attribute called "outline" that contains a 2d array of exactly {{sections_ammount}} items, each item is an flat array with top level (h2) headline of an article paragraph, also the array includes the id of a linking article which has a similar topic as the h2 headline. The value of the id has too be from the article Database bellow. Don't include the word "h2" in any of the titles, don't wrap titles in quotation marks, each heading is only the text of the heading. and another attribute called "categories" that contains a flat array of exactly{{categories_ammount}} items. Each item is a short SEO category, used to group different articles. The array will contain string items only. Add another attribute called "faq" that contains a 2d array of exactly {{faq_ammount}} items. Each item is an array with a question of a small paragraph and an answer on a topic from the article, do not include linking in faq. The arrays will contain string items only. add another attribute called "excerpt": which is a string with a very short description/excerpt (only for search engines to index) of the article and the main paragraphs topics, use exactly 20-30 words.

----------


structure of paragraph item: ["paragraph_title", linking_article_id] 
structure of the faq item: ["question", "answer"]

faq question examples: 
{{faq_examples}}

DO NOT use the faq examples in the faq output.


Linking Article Database:
ID, Title
{{linking_articles_database}}





The value of the id has too be from the article Database bellow. Don't include the word "h2" in any of the titles, don't wrap titles in quotation marks, each heading is only the text of the heading. 